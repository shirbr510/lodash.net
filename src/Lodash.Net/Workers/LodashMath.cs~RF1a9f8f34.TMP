using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection;
using Lodash.Net.Workers.Abstract;

namespace Lodash.Net.Workers
{
    public class LodashMath : ILodashMath
    {
        public double Add(double augend, double addend) => augend + addend;

        public double Ceil(double number, int precision = 0)
        {
            var precisionFactor = Math.Pow(10, precision);
            return Math.Ceiling(number * precisionFactor) / precisionFactor;
        }

        public double Divide(double dividend, double divisor) => dividend / divisor;

        public double Floor(double number, int precision = 0)
        {
            var precisionFactor = Math.Pow(10, precision);
            return Math.Floor(number * precisionFactor) / precisionFactor;
        }

        public T Max<T>(IEnumerable<T> enumerable)
        {
            return enumerable.Max();
        }

        public TResult MaxBy<TSource, TResult>(IEnumerable<TSource> enumerable, Func<TSource, TResult> iteratee)
        {
            return enumerable.Max(iteratee);
        }

        public T MaxBy<T, TResult>(IEnumerable<T> enumerable, string iteratee)
        {
            Func<T, object> func = item => typeof(T).GetTypeInfo().GetProperty(iteratee).GetValue(item);
            return (T)MaxBy(enumerable, func);
        }

        public T Mean<T>(IEnumerable<T> enumerable)
        {
            throw new NotImplementedException();
        }

        public T MeanBy<T>(IEnumerable<T> enumerable, Func<T, object> iteratee)
        {
            throw new NotImplementedException();
        }

        public T MeanBy<T>(IEnumerable<T> enumerable, string iteratee)
        {
            throw new NotImplementedException();
        }

        public T Min<T>(IEnumerable<T> enumerable)
        {
            throw new NotImplementedException();
        }

        public T MinBy<T>(IEnumerable<T> enumerable, Func<T, object> iteratee)
        {
            throw new NotImplementedException();
        }

        public T MinBy<T>(IEnumerable<T> enumerable, string iteratee)
        {
            throw new NotImplementedException();
        }

        public double Multiply(double multiplier, double multiplicand)
        {
            throw new NotImplementedException();
        }

        public double Round(double number, int precision = 0)
        {
            var precisionFactor = Math.Pow(10, precision);
            return Math.Round(number * precisionFactor) / precisionFactor;
        }

        public double Substract(double minuend, double subtrahend)
        {
            throw new NotImplementedException();
        }

        public T Sum<T>(IEnumerable<T> enumerable)
        {
            throw new NotImplementedException();
        }

        public T SumBy<T>(IEnumerable<T> enumerable, Func<T, object> iteratee)
        {
            throw new NotImplementedException();
        }

        public T SumBy<T>(IEnumerable<T> enumerable, string iteratee)
        {
            throw new NotImplementedException();
        }
    }
}
